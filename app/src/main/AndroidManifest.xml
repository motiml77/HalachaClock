<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Location (zmanim are location-dependent) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Exact alarms (Android 12+: SCHEDULE requires user grant, USE is auto-granted for alarm apps) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Draw-over-apps: lets the ringing screen open even while the phone is
         unlocked and in use (Android otherwise demotes it to a heads-up).
         Optional — requested in onboarding; everything else works without. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Network (ChaiTables visible sunrise tables) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Alarm reliability under Doze -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".ZmanimApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZmanimClock"
        android:localeConfig="@xml/locales_config"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Full-screen ringing UI, shown over the lock screen.
             directBootAware so it can show before first unlock after reboot. -->
        <activity
            android:name=".feature.alarm.presentation.AlarmActivity"
            android:exported="false"
            android:directBootAware="true"
            android:launchMode="singleInstance"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.ZmanimClock" />

        <!-- Re-arm alarms after boot / update / clock or timezone change.
             LOCKED_BOOT_COMPLETED fires before first unlock (Direct Boot). -->
        <receiver
            android:name=".scheduling.BootReceiver"
            android:exported="true"
            android:enabled="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Exact-alarm firing point -->
        <receiver
            android:name=".scheduling.AlarmTriggerReceiver"
            android:exported="false"
            android:directBootAware="true" />

        <!-- Persistent "next zman / next alarm" status notification updater -->
        <receiver
            android:name=".scheduling.StatusNotificationReceiver"
            android:exported="false" />

        <!-- Wake-up check (are you awake?) + its confirm action -->
        <receiver
            android:name=".scheduling.WakeCheckReceiver"
            android:exported="false"
            android:directBootAware="true" />
        <receiver
            android:name=".scheduling.WakeCheckConfirmReceiver"
            android:exported="false"
            android:directBootAware="true" />

        <!-- Foreground service that owns the ringing alarm -->
        <service
            android:name=".scheduling.AlarmSoundService"
            android:exported="false"
            android:directBootAware="true"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Home-screen widget: selected zmanim + next-zman live countdown -->
        <receiver
            android:name=".feature.widget.ZmanWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/zman_widget_info" />
        </receiver>

        <activity
            android:name=".feature.widget.WidgetConfigActivity"
            android:exported="false"
            android:theme="@style/Theme.ZmanimClock">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Disable default WorkManager initializer (initialized via Hilt) -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
